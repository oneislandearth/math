// Import the round function
import { round } from './round';

// Describe the add function
export const addition = (a, b) => round(a + b);

// Export the shorthand function
export const add = addition;