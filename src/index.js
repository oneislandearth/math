// Import each of the functions
import { absolute, abs } from './absolute';
import { cosine, cos } from './cosine';
import { round, rnd } from './round';

// Export each of the functions
export {
  absolute, abs,
  cosine, cos,
  round, rnd,
}